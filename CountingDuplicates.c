#include <stddef.h>
#include <ctype.h>
#include <string.h>

size_t duplicate_count(const char* text) 
{
  // create a lowercase version of the string for easier counting
  for(int i = 0; i < strlen(text); i++); 
}